export const siteConfig = {
  name: "TemplateForge",
  description: "Production-ready Next.js template starter for scalable resale businesses.",
  url: "https://example.com",
} as const;

export type SiteConfig = typeof siteConfig;
