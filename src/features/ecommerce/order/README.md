# Ecommerce Order Feature

Scalable order management feature module with:

- `AdminOrdersPage`: dashboard-style order list
- `AdminOrderDetailPage`: order detail view
- `OrderTable`: reusable table component with row click navigation
- `OrderStatusBadge`: status chip component
- `useOrders` and `useOrderById`: repository-driven hooks

The module intentionally does not include authentication logic.
